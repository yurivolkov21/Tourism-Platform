# 06-specs/

Per-feature **design specs** (the "what" + "why"), one file per feature:
`<YYYY-MM-DD>-<kebab-name>-design.md`. Written before code in the
spec→plan→execute flow (see `/new-feature`). Pairs with a plan in `../07-plans/`.
