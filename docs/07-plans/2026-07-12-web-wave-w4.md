# Web wave W4 — implementation plan

**Spec:** `docs/06-specs/2026-07-12-web-wave-w4-design.md`
**Branch:** `feat/web-wave-w4` · **Scope:** apps/web only

**STATUS: ✅ COMPLETE** — merged to `main` 2026-07-12 (`ff058e9`, ff-only);
web **261** tests. Adversarial review: 0 block-merge, 2 should-fix hardenings
folded in (aria-clobber lock + native-required guard). **This closes the web
debt program W1→W4.**

Standing rules: TDD-first (component spec red before implementation) ·
byte-equivalent DOM on migrations · straight quotes · tokens only · imports
relative · kill orphan node before nx runs · adversarial review before merge ·
Conventional Commits, no AI attribution.

## Tasks

### T1 — `AuthFormField` (TDD)

- [ ] `components/auth/auth-form-field.spec.tsx` first (jest.mock the
      `@tourism/ui` barrel like trust-band.spec): id/label wiring ·
      aria-required gating · error state (invalid + describedby + message) ·
      omitted field/code renders no error artifacts · hint/after slots ·
      children escape hatch. Run red.
- [ ] `components/auth/auth-form-field.tsx` per the spec's prop table. Green.

### T2 — Migrate 16 field groups

- [ ] register ×4 · login ×2 (`after` = forgot link) · reset ×2 · forgot ×1 ·
      change-email ×2 · change-password ×1 (confirm) · profile ×3 (hint on
      email). new-password composite stays bespoke. Remove now-dead imports.
      Existing suites stay green.

### T3 — noValidate + titles

- [ ] `noValidate` on: profile-form · booking-actions cancel-reason ·
      hero search · blog search.
- [ ] login/register metadata → `messages.auth.{login,register}.title`.

### T4 — Gate + review + merge

- [ ] Kill orphan node → `pnpm nx affected -t lint typecheck test build
      --base=main` + `pnpm format:check` green.
- [ ] Adversarial review (DOM parity per migrated site · aria regression ·
      the after/hint/children slots · forms sweep completeness).
- [ ] Report → user confirms → commit → ff-merge → push → sweep (changelog
      entry closes the W1→W4 program · frontend.md · CLAUDE.md web row ·
      HANDOFF · plan/spec STATUS).
