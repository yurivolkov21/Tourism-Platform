# Web wave W1 — implementation plan

**Spec:** `docs/06-specs/2026-07-12-web-wave-w1-design.md`
**Branch:** `feat/web-wave-w1` · **Scope:** apps/web + @tourism/i18n (no BE, no migration)

**STATUS: ✅ COMPLETE** — merged to `main` 2026-07-12 (`ce8da9e`, ff-only). No
BE changes, no migration. Gate green: web **247** (+15). Adversarial review:
0 block-merge; 4 should-fix + 2 nits fixed pre-merge (stuck-submitting catch ·
real radiogroup keyboard semantics · suitableFor added to the /tours
TourListCard tag row · crafted-input totality · unknown-enum mapper filter ·
double-submit guard); 1 nit accepted (groupSize edge normalization matches
PlanTrip). Next: **W2** (loading/error/404 resilience layer).

Standing rules: TDD-first on pure logic · `noValidate` + per-field server
errors on every form · tokens only, reuse `@tourism/ui` · copy in
`@tourism/i18n` · straight quotes · kill orphan node before nx runs ·
adversarial review before merge · Conventional Commits, no AI attribution.

## Tasks

### T1 — Review form (TDD)

- [ ] `lib/forms/validate` additions: `validateReviewFields` spec first
      (rating 1–5 required · body 10–2000 required · title ≤120 optional).
- [ ] `lib/api/reviews.ts` `createReview` (authedJson POST `/api/v1/reviews`)
      + `lib/reviews/actions.ts` `submitReview` server action
      (server-side re-validation → API → `{ ok, fieldErrors?, error? }`,
      error-code mapping incl. 409 → `alreadyReviewed: true`).
- [ ] `components/booking/review-prompt.tsx` (`'use client'`, noValidate):
      star radiogroup · title · body · submit; success/already-reviewed inline
      panels; failure toasts; i18n `messages.reviews.*` +
      `messages.fieldErrors` additions.
- [ ] Wire into `account/bookings/[code]/page.tsx` (PAID only, after
      `<BookingActions>`).

### T2 — suitableFor chips (TDD mappers)

- [ ] `toTourCard`/`toTourDetail` map `suitableFor ?? []`; extend
      `TourCardData` + `TourDetailVM`; mapper specs first.
- [ ] Detail hero "Ideal for" chip row (hidden when empty) + compact card row;
      i18n `messages.tours.suitableFor` + `messages.travellerTypes`.

### T3 — Contact lead fields (TDD payload)

- [ ] Extend `lib/enquiry-form.spec.ts` (contact payload w/ nationality/
      travelDate/groupSize/budgetTier; groupSize normalization) → extend
      `buildContactPayload`.
- [ ] Lift `ChoiceChips` → `components/marketing/choice-chips.tsx`
      (plan-trip-form imports it; behavior unchanged).
- [ ] `contact-inquiry.tsx` + 4 optional fields (Input · DatePicker · number
      Input · ChoiceChips) + `messages.contact.inquiry.form.*` keys.

### T4 — Gate + review + merge

- [ ] Kill orphan node → `pnpm nx affected -t lint typecheck test build
      --base=main` green + `pnpm format:check`.
- [ ] Adversarial review (review-form flow: auth/error mapping/409 UX ·
      validation parity with BE limits · chips edge cases · contact payload
      regressions) — fix confirmed findings.
- [ ] Report → user confirms → commit → ff-merge → push → delete branch →
      changelog entry + docs sweep (rule 9: frontend.md routes/account rows ·
      roadmap W1 note if debt program tracked there · CLAUDE.md web row).
