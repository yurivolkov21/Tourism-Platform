# plans/

Per-feature **implementation plans** (the "how" — dependency-ordered tasks with
an acceptance check each), one file per feature: `<YYYY-MM-DD>-<kebab-name>.md`.
Written after the design spec, executed task-by-task (TDD on logic). See `/new-feature`.
