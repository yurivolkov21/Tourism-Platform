# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# Build outputs (per-project) — generated, never format
**/dist/
**/.next/
**/.expo/

# Lockfiles are managed by the package manager — never reformat
/pnpm-lock.yaml