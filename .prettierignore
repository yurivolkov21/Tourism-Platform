# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# Build outputs (per-project) — generated, never format
**/dist/
**/.next/
**/.expo/

# Framework-generated files (overwritten on every build) — never format
**/next-env.d.ts

# Lockfiles are managed by the package manager — never reformat
/pnpm-lock.yaml
# Brainstorm/SDD scratch
/.superpowers/
