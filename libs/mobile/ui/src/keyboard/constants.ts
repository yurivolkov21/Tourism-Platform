/** Comfortable gap between the focused field and the keyboard (24–40px range). */
export const KEYBOARD_SAFETY_PADDING = 32;

/** Extra padding for multiline / last-field clusters (message + actions). */
export const KEYBOARD_SAFETY_PADDING_LARGE = 40;

/** Scroll animation duration aligned with native keyboard open (~250–300ms). */
export const KEYBOARD_SCROLL_ANIM_MS = 280;
