/**
 * Live API origin (no `/api/v1`) — same data as web on Vercel.
 * Used by app.config.js; keep in sync with src/lib/api/constants.ts.
 */
module.exports.PRODUCTION_API_BASE_URL =
  'https://tourism-api-pqwr.onrender.com';
